import dotenv from 'dotenv';
import express from 'express';
import cors from 'cors';
import http from 'http';
import path from 'path';
import fs from 'fs';
import { fileURLToPath } from 'url';
import { startWebSocketServer } from './websocket.js';
import { startMqttClient } from './mqtt.js';
import apiRouter from './routes.js';
import { dbEnabled, dbPingDetailed } from './db.js';
// Load .env (if present). In production you can provide env vars directly.
dotenv.config();
// config
// Many hosting providers expose the listening port via PORT.
// Default changed from 3000 -> 8080 (some hosts block 3000).
// You can still override via PORT or API_PORT.
const API_PORT = Number(process.env.PORT ?? process.env.API_PORT ?? 8080);
// WS behaviour:
// - If WS_PORT is set => run WS on a separate port (legacy/local).
// - Otherwise => attach WS to the same HTTP server at /ws (deploy-friendly).
const WS_PORT = process.env.WS_PORT ? Number(process.env.WS_PORT) : undefined;
const WS_PATH = process.env.WS_PATH ?? '/ws';
const MQTT_URL = process.env.MQTT_URL ?? 'mqtt://broker.emqx.io:1883';
const TOPIC_SUB = process.env.TOPIC_SUB ?? 'hluan/aqm/+/telemetry';
const app = express();
// If you deploy frontend + backend under the same domain, CORS is not needed.
// Keep it permissive by default (can be restricted via CORS_ORIGIN).
const corsOrigin = String(process.env.CORS_ORIGIN ?? '').trim();
app.use(cors(corsOrigin ? { origin: corsOrigin.split(',').map(s => s.trim()) } : undefined));
app.use(express.json({ limit: '1mb' }));
app.use('/api', apiRouter);
// Serve the built frontend (optional)
// Expected location: projectRoot/dist (generated by `npm run build` at repo root)
const __dirname = path.dirname(fileURLToPath(import.meta.url));
const webDist = process.env.WEB_DIST
    ? path.resolve(process.env.WEB_DIST)
    : path.resolve(__dirname, '../../dist');
if (fs.existsSync(path.join(webDist, 'index.html'))) {
    console.log(`[WEB] Serving static from ${webDist}`);
    app.use(express.static(webDist));
    // SPA fallback (must be after /api)
    // Express v5 does not accept "*" as a path string, use RegExp instead.
    app.get(/.*/, (_req, res) => res.sendFile(path.join(webDist, 'index.html')));
}
else {
    console.log('[WEB] dist/ not found -> API-only mode');
}
const server = http.createServer(app);
server.listen(API_PORT, '0.0.0.0', () => console.log(`HTTP on ${API_PORT}`));
if (typeof WS_PORT === 'number' && Number.isFinite(WS_PORT)) {
    startWebSocketServer(WS_PORT);
}
else {
    startWebSocketServer({ server, path: WS_PATH });
}
startMqttClient(MQTT_URL, TOPIC_SUB);
// Log DB connectivity once at startup (helps diagnose "backend can't connect to DB").
void (async () => {
    if (!dbEnabled()) {
        console.log('[DB] MYSQL_* env not set -> running without MySQL persistence');
        return;
    }
    const r = await dbPingDetailed();
    if (r.ok)
        console.log('[DB] Connected');
    else
        console.error('[DB] Connection failed:', r.error);
})();
//# sourceMappingURL=index.js.map